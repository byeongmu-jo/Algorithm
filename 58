n = 1
ss[len(ss)-1]

if len(ss[len(ss)-n]) != 0:
    return(len(ss[len(ss)-n]))
else:
    while len(ss[len(ss)-n]) == 0:
    n = n +1
    if len(ss[len(ss)-n]) != 0:
        return(len(ss[len(ss)-n]))
